{% extends 'base.html.twig' %}

{% block title %}Outing Creation{% endblock %}

{% block body %}

    <div class="d-flex justify-content-center">
        <h1>New outing here! ✅</h1>
        <h2>Complete all the details, and confirm to create it!</h2>
        <div class="new-outing">
        {{ form_start(outingForm) }}
        {{ form_widget(outingForm) }}
        <br><button class="btn btn-primary">Confirm</button>
        {{ form_errors(outingForm) }}
        </div>
    </div>
    {{ form_end(outingForm) }}

{% endblock %}